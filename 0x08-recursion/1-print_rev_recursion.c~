#include "main.h"
#include <string.h>

/**
 * _print_rev_recursion - function that prints a string in reverse
 * @s: string to print in revers
 * Return: nothing
 */

void _print_rev_recursion(char *s)
{
	_putchar('s[unsigned int(strlen(s) - 1)]');
	s--;
	_print_rev_recursion(s);
}
