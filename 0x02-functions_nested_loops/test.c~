#include <stdio.h>
#include <unistd.h>
#include <string.h>

int main(void)
{
	char *str = "Saad";
	int i;
	int ln = strlen(str);
	
	int _putchar(char c)
	{
		return (write(1, &c, 1));
	}
	printf("%i\n",ln);
	for (i = 0; i <= ln; i++)
	{
		printf("A\n");
		_putchar(str[i]);
	}
	return (0);
}
